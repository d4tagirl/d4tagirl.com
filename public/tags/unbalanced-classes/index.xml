<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>unbalanced classes on d4tagirl</title>
    <link>/tags/unbalanced-classes/</link>
    <description>Recent content in unbalanced classes on d4tagirl</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <lastBuildDate>Mon, 23 Jan 2017 22:00:53 +0000</lastBuildDate>
    
        <atom:link href="/tags/unbalanced-classes/index.xml" rel="self" type="application/rss+xml" />
    
    
    <item>
      <title>Using Rpart to figure out who voted for Trump</title>
      <link>/2017/01/rpart-who-voted-for-trump/</link>
      <pubDate>Mon, 23 Jan 2017 22:00:53 +0000</pubDate>
      
      <guid>/2017/01/rpart-who-voted-for-trump/</guid>
      <description>

&lt;p&gt;It&amp;rsquo;s been a few days since we witnessed the inauguration of Donald Trump as the 45th President of the United States, whose victory over Hillary Clinton came as a shock for most people. I&amp;rsquo;m not much into politics (and it is not even my country!) but this result really caught my attention, so I wanted to dig a little about the population&amp;rsquo;s characteristics that made him the winner of the Election.&lt;/p&gt;

&lt;p&gt;I&amp;rsquo;ve been studying Machine Learning for a while now, and a couple of months ago I discovered the &lt;em&gt;awesome&lt;/em&gt; &lt;code&gt;tidyverse&lt;/code&gt; world (I can&amp;rsquo;t believe the way I used to do things :$), so I thought this was a great opportunity to test my skills. In addition to that, I&amp;rsquo;m not a native English speaker so in this first post I am facing major challenges! If you see something improvable, not clear or plainly wrong, please leave a comment or &lt;a href=&#34;https://twitter.com/intent/tweet?user_id=114258616&#34;&gt;mention me on Twitter&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;What I do here is estimate a Classification Tree (CART) to find an association between the winner in the county and its socio-demographic characteristics.&lt;/p&gt;

&lt;h1 id=&#34;the-data&#34;&gt;The data&lt;/h1&gt;

&lt;p&gt;I recently joined Kaggle, and &lt;a href=&#34;https://www.kaggle.com/joelwilson&#34;&gt;Joel Wilson&lt;/a&gt; gathered data about &lt;em&gt;2016 US Election Results&lt;/em&gt; by county and &lt;em&gt;County Quick Facts&lt;/em&gt; from the &lt;em&gt;US Census Bureau&lt;/em&gt;. &lt;a href=&#34;https://www.kaggle.com/joelwilson/2012-2016-presidential-elections&#34;&gt;This is the data&lt;/a&gt; I use to run this analysis.&lt;/p&gt;

&lt;p&gt;I start by loading the data and merging it. No mystery here, except I load it using &lt;code&gt;readr&lt;/code&gt; and merge it using &lt;code&gt;dplyr&lt;/code&gt; (yay!).&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;library(readr)
library(dplyr)

url_pop &amp;lt;- &#39;https://github.com/d4tagirl/TrumpVsClintonCountiesRpart/raw/master/data/county_facts.csv&#39;
url_results &amp;lt;- &#39;https://github.com/d4tagirl/TrumpVsClintonCountiesRpart/raw/master/data/US_County_Level_Presidential_Results_12-16.csv&#39;  
pop &amp;lt;- read_csv(url(url_pop))
results &amp;lt;- read_csv(url(url_results))

votes &amp;lt;- results %&amp;gt;% 
  inner_join(pop, by = c(&amp;quot;combined_fips&amp;quot; = &amp;quot;fips&amp;quot;))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;It is a clean dataset, but I need to do some modifications for the analysis:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;There is no election data for the state of Alaska so I remove those counties.&lt;/li&gt;
&lt;li&gt;Replace the old ID (&lt;code&gt;X1&lt;/code&gt;) with a new one (&lt;code&gt;ID&lt;/code&gt;).&lt;/li&gt;
&lt;li&gt;Delete non relevant variables.&lt;/li&gt;
&lt;li&gt;Rename variables to make them interpretable.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;All of this taking advantage of the &lt;code&gt;dplyr&lt;/code&gt; of course.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;votes &amp;lt;- votes %&amp;gt;%
  filter(state_abbr != &amp;quot;AK&amp;quot;) %&amp;gt;%
  mutate(ID = rank(X1)) %&amp;gt;%
  select(-X1, -POP010210, -PST040210, -NES010213, -WTN220207) %&amp;gt;%
  rename(age18minus = AGE295214, age5minus = AGE135214, age65plus = AGE775214,
    american_indian = RHI325214, asian = RHI425214, asian_Firms = SBO215207,
    black = RHI225214, black_firms = SBO315207, building_permits = BPS030214,
    density = POP060210, edu_batchelors = EDU685213, edu_highschool = EDU635213,
    firms_num = SBO001207, foreign = POP645213, hisp_latin = RHI725214,
    hispanic_firms = SBO415207, home_owners_rate = HSG445213, households = HSD410213,
    housing_Units = HSG010214, housing_units_multistruct = HSG096213, income = INC910213,
    land_area = LND110210, living_same_house_12m = POP715213, manuf_ship = MAN450207,
    Med_house_income = INC110213, Med_val_own_occup = HSG495213, native_haw = RHI525214,
    native_firms = SBO115207, nonenglish = POP815213, pacific_isl_firms = SBO515207,
    pers_per_household = HSD310213, pop_change = PST120214, pop2014 = PST045214,
    poverty = PVY020213, priv_nofarm_employ = BZA110213, priv_nonfarm_employ_change = BZA115213,
    priv_nonfarm_estab = BZA010213, retail_sales = RTN130207, retail_sales_percap = RTN131207,
    sales_accomod_food = AFN120207, sex_f = SEX255214, travel_time_commute = LFE305213,
    two_races_plus = RHI625214, veterans = VET605213, white = RHI125214, white_alone = RHI825214,
    women_firms = SBO015207,
    Trump = per_gop_2016, Clinton = per_dem_2016, Romney = per_gop_2012, Obama = per_dem_2012)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Most of these variables are measured as a percentage of people in the county with that characteristic (e.g. &lt;code&gt;edu_batchelors&lt;/code&gt; and &lt;code&gt;black&lt;/code&gt;), or the total amount in the county (e.g. &lt;code&gt;land_area&lt;/code&gt; and &lt;code&gt;firms_num&lt;/code&gt;).&lt;/p&gt;

&lt;p&gt;Note that there is a &lt;code&gt;white&lt;/code&gt; variable and a &lt;code&gt;white_alone&lt;/code&gt; variable. This is because there are 2 separate facts gathered here.&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;p&gt;&lt;code&gt;white&lt;/code&gt; refers to &lt;em&gt;race&lt;/em&gt;: &lt;em&gt;people having origins in any of the original peoples of Europe, the Middle East, or North Africa&lt;/em&gt;. If a person declares &lt;em&gt;white&lt;/em&gt; among other races, is classified as &lt;code&gt;two_races_plus&lt;/code&gt; and not as &lt;code&gt;white&lt;/code&gt;.&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;&lt;code&gt;white_alone&lt;/code&gt; refers to &lt;em&gt;white race&lt;/em&gt; people who also reported &lt;em&gt;not Hispanic or Latino origin&lt;/em&gt;.&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;For further references on any variable you can &lt;a href=&#34;https://www.census.gov/quickfacts/fact/table/US/PST045218&#34;&gt;go to the Census Bureau&amp;rsquo;s site&lt;/a&gt;.&lt;/p&gt;

&lt;h1 id=&#34;building-the-response-variable&#34;&gt;Building the Response Variable&lt;/h1&gt;

&lt;p&gt;I create the variable I want to explain: &lt;code&gt;pref_cand_T&lt;/code&gt;. It takes the value &lt;code&gt;1&lt;/code&gt; if &lt;code&gt;Trump&lt;/code&gt; has a greater percentage of votes than &lt;code&gt;Clinton&lt;/code&gt; in the county, and &lt;code&gt;0&lt;/code&gt; otherwise. Note that it is not necessary that one of them has more than 50% of the votes, it&amp;rsquo;s only required that has more votes than the other.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;votes &amp;lt;- votes %&amp;gt;% mutate(pref_cand_T = factor(ifelse(Trump &amp;gt; Clinton, 1, 0)))

summary &amp;lt;- votes %&amp;gt;% summarize(Trump       = sum(pref_cand_T == 1),
                               Clinton     = n() - Trump,
                               Trump_per   = mean(pref_cand_T == 1),
                               Clinton_per = 1 - Trump_per)

library(knitr)
knitr::kable(summary, align = &#39;l&#39;)
&lt;/code&gt;&lt;/pre&gt;

&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th align=&#34;left&#34;&gt;Trump&lt;/th&gt;
&lt;th align=&#34;left&#34;&gt;Clinton&lt;/th&gt;
&lt;th align=&#34;left&#34;&gt;Trump_per&lt;/th&gt;
&lt;th align=&#34;left&#34;&gt;Clinton_per&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;

&lt;tbody&gt;
&lt;tr&gt;
&lt;td align=&#34;left&#34;&gt;2624&lt;/td&gt;
&lt;td align=&#34;left&#34;&gt;488&lt;/td&gt;
&lt;td align=&#34;left&#34;&gt;0.8431877&lt;/td&gt;
&lt;td align=&#34;left&#34;&gt;0.1568123&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Trump got more votes than Clinton in 2624 counties opposing the 488 other counties where Clinton got more votes (remember we are talking about counties and not Electoral College votes). The proportion is 84% for Trump and 16% for Clinton.&lt;/p&gt;

&lt;h1 id=&#34;some-visualization-about-race-and-origin&#34;&gt;Some visualization about race and origin&lt;/h1&gt;

&lt;p&gt;I explore briefly some counties&amp;rsquo; characteristics about &lt;em&gt;race&lt;/em&gt; and &lt;em&gt;origin&lt;/em&gt; by visualizing them. It is time to test my brand new &lt;code&gt;ggplot&lt;/code&gt; skills!&lt;/p&gt;

&lt;p&gt;I generate a plot for the mean of each characteristic across all counties, which is the mean of the proportion of people with each characteristic in all counties (simple mean, without considering the population of the county).&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;library(tidyr)
library(ggplot2)

# Order in x-axis
limits = c(&amp;quot;white&amp;quot;, &amp;quot;white_alone&amp;quot;, &amp;quot;black&amp;quot;, &amp;quot;asian&amp;quot;, &amp;quot;hisp_latin&amp;quot;, &amp;quot;foreign&amp;quot;)

total &amp;lt;- votes %&amp;gt;% 
  summarize(
    white       = mean(white),
    white_alone = mean(white_alone),
    black       = mean(black),
    asian       = mean(asian),
    hisp_latin  = mean(hisp_latin),
    foreign     = mean(foreign)) %&amp;gt;%
  gather(variable, value) %&amp;gt;% 
  ggplot() +
  geom_bar(aes(x = variable, y = value), 
           stat = &#39;identity&#39;, width = .7, fill = &amp;quot;#C9C9C9&amp;quot;) +
  geom_vline(xintercept = c(4.5, 5.5), alpha = 0.2 ) +
  scale_x_discrete(limits = limits) +
  labs(title = &amp;quot;Mean of % in counties&amp;quot;,    
       subtitle = &amp;quot;(Simple mean of % in counties without considering counties&#39; population)&amp;quot;) +
  theme_bw() +
  theme(axis.title.x = element_blank(), axis.title.y = element_blank(),
        axis.text.x = element_blank(), axis.line = element_line(colour = &amp;quot;grey&amp;quot;),
        panel.grid.major = element_blank(), panel.border = element_blank())
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Something beautiful about &lt;code&gt;tidyverse&lt;/code&gt; is the way that you can build up the solution. The first part is simply &lt;code&gt;dplyr&lt;/code&gt; plus &lt;code&gt;tidyr::gather&lt;/code&gt;, necessary to manipulate the input for the &lt;code&gt;ggplot&lt;/code&gt;. The rest is the &lt;code&gt;ggplot&lt;/code&gt; layer by layer.&lt;/p&gt;

&lt;p&gt;It seems like a lot of code for a simple plot, but trust me: once you get the hang of &lt;code&gt;ggplot&lt;/code&gt;, it is magic! The only fear is that you&amp;rsquo;ll want to customize &lt;em&gt;everything&lt;/em&gt;! (An that&amp;rsquo;s why I have so much code&amp;hellip;)&lt;/p&gt;

&lt;p&gt;Then I generate the same plot by candidate.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;by_candidate &amp;lt;- votes %&amp;gt;% 
  group_by(pref_cand_T) %&amp;gt;% 
  summarize(
    white       = mean(white),
    white_alone = mean(white_alone),
    black       = mean(black),
    asian       = mean(asian),
    hisp_latin  = mean(hisp_latin),
    foreign     = mean(foreign)) %&amp;gt;% 
  gather(variable, value, -pref_cand_T) %&amp;gt;% 
  ggplot() + 
  geom_bar(aes(x = variable, y = value, fill = pref_cand_T),
           stat = &#39;identity&#39;, position = &#39;dodge&#39;) + 
  geom_vline(xintercept = c(4.5, 5.5), alpha = 0.2 ) +
  scale_fill_manual(values = alpha(c(&amp;quot;blue&amp;quot;, &amp;quot;red&amp;quot;)), 
                    breaks = c(&amp;quot;0&amp;quot;, &amp;quot;1&amp;quot;), labels = c(&amp;quot;Clinton&amp;quot;, &amp;quot;Trump&amp;quot;)) +
  scale_x_discrete(limits = limits) +
  labs(fill = &amp;quot;winner&amp;quot;) +
  theme_bw() +
  theme(axis.title.x = element_blank(), axis.title.y = element_blank(),
        axis.line = element_line(colour = &amp;quot;grey&amp;quot;), legend.position = &amp;quot;bottom&amp;quot;, 
        panel.grid.major = element_blank(), panel.border = element_blank())
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Same as before, but excluding the grouping variable &lt;code&gt;pref_cand_T&lt;/code&gt; from the gathering to generate the &lt;code&gt;ggplot&lt;/code&gt; input.&lt;/p&gt;

&lt;p&gt;Next I plot both. I use the &lt;code&gt;gridExtra&lt;/code&gt; package to display both plots together. (And yes, it was a lot of learning this past few months!).&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;library(gridExtra)

grid.arrange(total, by_candidate, nrow = 2)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;img src=&#34;/post/what-demographics-voted-for-trump/2017-01-23-what-demographics-voted-for-trump/plot_race-1.png&#34; title=&#34;plot of chunk plot_race&#34; alt=&#34;plot of chunk plot_race&#34; style=&#34;margin: auto;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;Among races, the mean of &lt;em&gt;white&lt;/em&gt; people is higher for the counties where Trump won than the rest, and for the counties where Clinton won, the mean of &lt;em&gt;black&lt;/em&gt; and &lt;em&gt;asian&lt;/em&gt; people is higher. Clinton also won in counties with higher mean of &lt;em&gt;Hispanic or Latin origin&lt;/em&gt; people, and &lt;em&gt;foreign-born&lt;/em&gt; population.&lt;/p&gt;

&lt;p&gt;In a future post I will be doing some more digging into this variables to find out more.&lt;/p&gt;

&lt;h1 id=&#34;the-classification-tree&#34;&gt;The Classification Tree&lt;/h1&gt;

&lt;p&gt;As a standard practice, I split the data into &lt;code&gt;train&lt;/code&gt; and &lt;code&gt;test&lt;/code&gt; samples. I use 70% of the counties for training and the rest for testing. Since I am dealing with unbalanced data, I use the &lt;code&gt;createDataPartition&lt;/code&gt; function in the &lt;code&gt;caret&lt;/code&gt; package that conserves the proportion of classes across all samples.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;library(caret)

perc_train &amp;lt;- 0.7
set.seed(3333)

trainIndex &amp;lt;- createDataPartition(y = votes$pref_cand_T, p = perc_train,
                                  list = FALSE, times = 1)

train &amp;lt;- votes %&amp;gt;% subset(ID %in% trainIndex)
test &amp;lt;-  votes %&amp;gt;% setdiff(train)
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;growing-the-tree&#34;&gt;Growing the tree&lt;/h2&gt;

&lt;p&gt;I estimate the tree using &lt;code&gt;rpart&lt;/code&gt;, excluding variables non relevant for modelling, and those used to build the response variable.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;library(rpart)

pref_cand_rpart &amp;lt;- rpart(pref_cand_T ~ .,
                         data = train[, -c(1:24, 70)],
                         control = rpart.control(xval = 10, cp = 0.0001))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;This algorithm grows a tree from a root that has all the observations, splitting binarily to reduce the impurity of its nodes, until some stopping rule is met. I set up this rules with the &lt;code&gt;rpart.control&lt;/code&gt;:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;code&gt;minsplit = 20&lt;/code&gt; is the default, the minimum of observations in a node to attempt a split.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;minbucket = 7&lt;/code&gt; is also the default, the minimum of observations in any terminal node.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;cp = 0.0001&lt;/code&gt; is the minimum factor of decreasing lack of fit for a split to be attempted.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;This is quite a big tree because I use a very small &lt;code&gt;cp&lt;/code&gt; (I&amp;rsquo;m not showing it for that reason, but you can &lt;a href=&#34;/post/what-demographics-voted-for-trump/2017-01-23-what-demographics-voted-for-trump/plot_large_tree-1.png&#34;&gt;find it here&lt;/a&gt;). Simpler trees are preferred, since they are less likely to overfit the data.&lt;/p&gt;

&lt;h2 id=&#34;pruning-the-tree&#34;&gt;Pruning the tree&lt;/h2&gt;

&lt;p&gt;Now it&amp;rsquo;s time to prune the tree. To do this, I keep the split if it meets some criteria about the balance between the impurity reduction (&lt;em&gt;cost&lt;/em&gt;) and the increase of size (&lt;em&gt;complexity&lt;/em&gt;). This is the &lt;code&gt;cp&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;I am pruning the tree following the 1-SE rule, according to which I choose the simplest model with accuracy similar to the best model.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;library(tibble)
cp &amp;lt;- as_tibble(pref_cand_rpart$cptable) %&amp;gt;%
  filter(xerror &amp;lt;= min(xerror) + xstd) %&amp;gt;%
  filter(xerror == max(xerror)) %&amp;gt;%
  select(CP) %&amp;gt;%
  unlist()

winner_rpart &amp;lt;- prune(pref_cand_rpart, cp = cp)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;dplyr&lt;/code&gt; manipulates &lt;code&gt;dataframes&lt;/code&gt;, so I use the &lt;code&gt;tibble::as_tibble&lt;/code&gt; function to convert the &lt;code&gt;pref_cand_rpart$cptable&lt;/code&gt; table to a &lt;code&gt;tibble&lt;/code&gt;, and then select the &lt;code&gt;cp&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;And here I have the tree! (If you know some other way to plot nicer trees, please comment!)&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;library(rpart.plot)

rpart.plot(winner_rpart, main = &amp;quot;Winner candidate in county&amp;quot;,
           extra = 104, split.suffix = &amp;quot;%?&amp;quot;, branch = 1,
           fallen.leaves = FALSE, box.palette = &amp;quot;BuRd&amp;quot;,
           branch.lty = 3, split.cex = 1.2,
           shadow.col = &amp;quot;gray&amp;quot;, shadow.offset = 0.2)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;img src=&#34;/post/what-demographics-voted-for-trump/2017-01-23-what-demographics-voted-for-trump/plot_pruned_tree-1.png&#34; title=&#34;plot of chunk plot_pruned_tree&#34; alt=&#34;plot of chunk plot_pruned_tree&#34; style=&#34;margin: auto;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;Starting from the top (the root), the tree splits the population in two subsets according to the question asked (the variable and the cut), and it does the same for every node. If the county meets the criteria it is classified to the left, and otherwise to the right. In this case the first split indicates that if the county has less than 50% &lt;code&gt;white_alone&lt;/code&gt; population, it is classified to the left node (only 12% of the counties), and the rest goes to the right (80% of the counties). The higher the percentage of counties that Trump won in the node, the redder the node. Nodes associated with Clinton are bluer.&lt;/p&gt;

&lt;p&gt;Apparently &lt;em&gt;race&lt;/em&gt; is one of the most important characteristic determining the winner candidate. 3 over the 5 splits include race (we will know more about this when we check the &lt;em&gt;Variable Importance&lt;/em&gt; later on). And there is also a variable referring the &lt;em&gt;housing structure&lt;/em&gt; and other about &lt;em&gt;education&lt;/em&gt;.&lt;/p&gt;

&lt;p&gt;One key feature of CART is that it allows different characteristics to be relevant for each node resulted from a split. In this case, for counties with less than 50% &lt;code&gt;white_alone&lt;/code&gt; population, the winner was also determined by the amount of housing units in multi-unit structures (maybe an urbanization&amp;rsquo;s proxy?) and the percentage of 25 years old or older persons holding a Bachelor&amp;rsquo;s degree or higher. But for the rest of the counties, it was determined by other racial characteristics. This is because the algorithm partitions the feature space in two using linear parallel to the axis decision boundaries, and for each generated region does the same, over and over.&lt;/p&gt;

&lt;p&gt;This can be revealing, uncovering underlying interactions of variables for different groups, harder to discover using other methods.&lt;/p&gt;

&lt;h1 id=&#34;performance-evaluation&#34;&gt;Performance Evaluation&lt;/h1&gt;

&lt;p&gt;Now I evaluate how good this model is. Prior to this post I wouldn&amp;rsquo;t pay any extra attention to the usage of different measures taking into account how unbalanced the data was. There are a lot of measures to evaluate the fit, I will explore some of them prioritizing the ones that explicitly deal with unbalanced classes.&lt;/p&gt;

&lt;p&gt;This evaluations should always be done over the &lt;code&gt;test&lt;/code&gt; sample, otherwise it would be like cheating!&lt;/p&gt;

&lt;h2 id=&#34;missclassification-error&#34;&gt;Missclassification Error&lt;/h2&gt;

&lt;p&gt;One classic way to evaluate the performance of a classifier is by calculating the &lt;em&gt;missclassification error&lt;/em&gt;, simply by computing the percentage of times that the the classifier is wrong.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;test &amp;lt;- test %&amp;gt;%
  mutate(pred = predict(winner_rpart, type = &amp;quot;class&amp;quot;, test),
         pred_prob_T = predict(winner_rpart, type = &amp;quot;prob&amp;quot;, test)[,2],
         error = ifelse(pred != pref_cand_T, 1, 0))

missc_error &amp;lt;- test %&amp;gt;% summarize(missc_error = mean(error))
knitr::kable(missc_error, align = &#39;l&#39;)
&lt;/code&gt;&lt;/pre&gt;

&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th align=&#34;left&#34;&gt;missc_error&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;

&lt;tbody&gt;
&lt;tr&gt;
&lt;td align=&#34;left&#34;&gt;0.0782422&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;But since I&amp;rsquo;ve been doing my research, it has become clear that in this case I could not treat this measure without further considerations.&lt;/p&gt;

&lt;p&gt;Let&amp;rsquo;s suppose I have a model that predicts for all cases Trump as the winner. It would have a missclassification error of 15%, &lt;em&gt;not so bad!&lt;/em&gt; But it is definitely not a great model: it would be 100% accurate for predicting counties where Trump won, but it would be wrong for &lt;em&gt;all&lt;/em&gt; of the counties where Clinton won. This is known as the &lt;em&gt;Accuracy Paradox&lt;/em&gt;, and that is why we need some alternatives to measure how good is this tree in predicting the county&amp;rsquo;s winner.&lt;/p&gt;

&lt;h2 id=&#34;kappa-statistic&#34;&gt;Kappa statistic&lt;/h2&gt;

&lt;p&gt;One good thing to do in any case (balanced the data or not) is to take a look at the confusion matrix. This is the input for many performance measures as shown next.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;test %&amp;gt;%
  select(pred, pref_cand_T) %&amp;gt;%
  table() %&amp;gt;%
  confusionMatrix()
&lt;/code&gt;&lt;/pre&gt;

&lt;pre&gt;&lt;code&gt;## Confusion Matrix and Statistics
## 
##     pref_cand_T
## pred   0   1
##    0  87  14
##    1  59 773
##                                           
##                Accuracy : 0.9218          
##                  95% CI : (0.9026, 0.9382)
##     No Information Rate : 0.8435          
##     P-Value [Acc &amp;gt; NIR] : 6.196e-13       
##                                           
##                   Kappa : 0.6611          
##  Mcnemar&#39;s Test P-Value : 2.607e-07       
##                                           
##             Sensitivity : 0.59589         
##             Specificity : 0.98221         
##          Pos Pred Value : 0.86139         
##          Neg Pred Value : 0.92909         
##              Prevalence : 0.15648         
##          Detection Rate : 0.09325         
##    Detection Prevalence : 0.10825         
##       Balanced Accuracy : 0.78905         
##                                           
##        &#39;Positive&#39; Class : 0               
## 
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Just looking into this matrix we can have some clues on how good is the classifier for each class. Particularly I want to look closer at the &lt;code&gt;Kappa&lt;/code&gt; statistic, because it specifically considers the unbalance between classes.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;Kappa&lt;/code&gt; measures the accuracy of the classifier &lt;em&gt;corrected by the probability of agreement by chance&lt;/em&gt;. There is not much consensus on the magnitude of &lt;code&gt;Kappa&lt;/code&gt; to consider it low or high, but it can be interpreted as how separate it is the obtained result from chance. In this case the &lt;em&gt;expected accuracy&lt;/em&gt; (the one that occurs by chance) is 77% and the perfect accuracy is of course 100%. There is a gap of 23%, and this classifier closes this gap by 66% of it (15% of improvement!). The higher the &lt;code&gt;Kappa&lt;/code&gt;, the better. Some sustain that a value of &lt;code&gt;Kappa&lt;/code&gt; larger than 60% means a good agreement, so we are OK!&lt;/p&gt;

&lt;h2 id=&#34;roc-curve-and-auroc&#34;&gt;ROC Curve and AUROC&lt;/h2&gt;

&lt;p&gt;To complement the performance evaluation, I check the &lt;code&gt;ROC curve&lt;/code&gt;. It plots the &lt;em&gt;True Positive Rate&lt;/em&gt; against the &lt;em&gt;False Positive Rate&lt;/em&gt; across many different thresholds of predicted probability. As we are evaluating a tree with only 6 final nodes, there is a limited amount of thresholds. (Trying to simplify this explanation, I came across &lt;a href=&#34;https://www.dataschool.io/roc-curves-and-auc-explained/&#34;&gt;this video&lt;/a&gt; that is very clear if you want to go deeper)&lt;/p&gt;

&lt;p&gt;This measure is great for classification analysis, and it is particularly useful here because it is not affected by unbalanced classes. Luckily I came across this great &lt;code&gt;ggplot2&lt;/code&gt; extension, &lt;code&gt;plotROC&lt;/code&gt;, and now I can use my favorite tools to create a pretty nice plot!&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;library(plotROC)

roc &amp;lt;- test %&amp;gt;%
  select(pref_cand_T, pred_prob_T) %&amp;gt;%
  mutate(pref_cand_T = as.numeric(pref_cand_T) - 1,
         pref_cand_T.str = c(&amp;quot;Clinton&amp;quot;, &amp;quot;Trump&amp;quot;)[pref_cand_T + 1]) %&amp;gt;%
  ggplot(aes(d = pref_cand_T, m = pred_prob_T)) +
  geom_roc(labels = FALSE)

roc +
style_roc(theme = theme_bw, xlab = &amp;quot;False Positive Rate&amp;quot;, ylab = &amp;quot;True Positive Rate&amp;quot;) +
theme(panel.grid.major = element_blank(), panel.border = element_blank(),
      axis.line = element_line(colour = &amp;quot;grey&amp;quot;)) +
ggtitle(&amp;quot;ROC Curve for winner_rpart classifier&amp;quot;) +
annotate(&amp;quot;text&amp;quot;, x = .75, y = .25,
         label = paste(&amp;quot;AUROC =&amp;quot;, round(calc_auc(roc)$AUC, 2)))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;img src=&#34;/post/what-demographics-voted-for-trump/2017-01-23-what-demographics-voted-for-trump/roc_curve-1.png&#34; title=&#34;plot of chunk roc_curve&#34; alt=&#34;plot of chunk roc_curve&#34; style=&#34;margin: auto;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;The &lt;code&gt;AUROC&lt;/code&gt; (*A*rea *U*nder the &lt;em&gt;ROC&lt;/em&gt; curve) computes the probability that the classifier ranks higher a positive instance than a negative one.&lt;/p&gt;

&lt;h1 id=&#34;warning-about-classification-trees-instability&#34;&gt;Warning about Classification Trees instability&lt;/h1&gt;

&lt;p&gt;When you use this kind of Machine Learning algorithm you should be aware that small changes in the data can change the tree. Find below a second tree generated by using the same parameters than before, but changing the seed to do the sampling.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;set.seed(4444)

trainIndex_2 &amp;lt;- createDataPartition(y = votes$pref_cand_T, p = perc_train,
                                    list = FALSE, times = 1)

train_2 &amp;lt;- votes %&amp;gt;% subset(ID %in% trainIndex_2)
test_2 &amp;lt;-  votes %&amp;gt;% setdiff(train_2)

winner_rpart_2 &amp;lt;- rpart(pref_cand_T ~ .,
                         data = train_2[, -c(1:24, 70)],
                         control = rpart.control(xval = 10, cp = cp))

rpart.plot(winner_rpart_2, main = &amp;quot;Winner candidate in county&amp;quot;,
           extra = 104, split.suffix = &amp;quot;%?&amp;quot;, branch = 1,
           fallen.leaves = FALSE, box.palette = &amp;quot;BuRd&amp;quot;,
           branch.lty = 3, split.cex = 1.2,
           shadow.col = &amp;quot;gray&amp;quot;, shadow.offset = 0.2)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;img src=&#34;/post/what-demographics-voted-for-trump/2017-01-23-what-demographics-voted-for-trump/second_tree-1.png&#34; title=&#34;plot of chunk second_tree&#34; alt=&#34;plot of chunk second_tree&#34; style=&#34;margin: auto;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;Although it is not completely different, it changes a bit.&lt;/p&gt;

&lt;p&gt;You can fight this using some ensemble method such as &lt;em&gt;Bagging&lt;/em&gt;, &lt;em&gt;Random Forest&lt;/em&gt; or &lt;em&gt;Boosting&lt;/em&gt;, but as I&amp;rsquo;m particularly interested in easily visualizing the decision rules and potentially finding different patterns for different groups, I find this method a better fit for this particular problem.&lt;/p&gt;

&lt;p&gt;Since this algorithm is very sensitive to the &lt;code&gt;train&lt;/code&gt; and &lt;code&gt;test&lt;/code&gt; partition, I&amp;rsquo;m interested in having an estimation of the &lt;em&gt;accuracy&lt;/em&gt; for &lt;em&gt;this kind of specification of the tree&lt;/em&gt; and not only for the specific one I found. This can be achieved by the K-fold Cross-Validation.&lt;/p&gt;

&lt;h2 id=&#34;k-fold-cross-validation-for-model-accuracy-estimation&#34;&gt;K-fold Cross Validation for model accuracy estimation&lt;/h2&gt;

&lt;p&gt;This method divides randomly the data into &lt;code&gt;k&lt;/code&gt; equally sized subsets called &lt;em&gt;folds&lt;/em&gt;. A tree is estimated in &lt;code&gt;k-1&lt;/code&gt; of them and the performance is evaluated in the fold left behind. This is made &lt;code&gt;k&lt;/code&gt; times, until each fold is used for validation exactly one time.&lt;/p&gt;

&lt;p&gt;This was a non a straightforward thing for me to do. I wanted the &lt;code&gt;caret&lt;/code&gt; package to calculate the K-fold Cross Validation accuracy estimation, but apparently most people use &lt;code&gt;caret::train&lt;/code&gt; to find the parameter &lt;code&gt;cp&lt;/code&gt; at the same time. I just wanted to estimate the accuracy associated with an already specified model (I&amp;rsquo;ve already estimated the &lt;code&gt;cp&lt;/code&gt;). Finally I found the way, specifying &lt;code&gt;tuneGrid = expand.grid(cp = cp)&lt;/code&gt;.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;tc &amp;lt;- trainControl(&amp;quot;cv&amp;quot;, number = 10)
rpart.grid &amp;lt;- expand.grid(cp = cp)
train.rpart &amp;lt;- train(pref_cand_T ~ .,
                     data = votes[,-c(1:24, 70)], method = &amp;quot;rpart&amp;quot;,
                     trControl = tc,  tuneGrid = rpart.grid)

knitr::kable(train.rpart$results, align = &amp;quot;l&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;

&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th align=&#34;left&#34;&gt;cp&lt;/th&gt;
&lt;th align=&#34;left&#34;&gt;Accuracy&lt;/th&gt;
&lt;th align=&#34;left&#34;&gt;Kappa&lt;/th&gt;
&lt;th align=&#34;left&#34;&gt;AccuracySD&lt;/th&gt;
&lt;th align=&#34;left&#34;&gt;KappaSD&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;

&lt;tbody&gt;
&lt;tr&gt;
&lt;td align=&#34;left&#34;&gt;0.0380117&lt;/td&gt;
&lt;td align=&#34;left&#34;&gt;0.9222377&lt;/td&gt;
&lt;td align=&#34;left&#34;&gt;0.6869&lt;/td&gt;
&lt;td align=&#34;left&#34;&gt;0.0111048&lt;/td&gt;
&lt;td align=&#34;left&#34;&gt;0.0525811&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The &lt;code&gt;Kappa&lt;/code&gt; statistic is now 0.69 (it was also 0.66 for the &lt;code&gt;winner_rpart&lt;/code&gt;), with a 0.05 standard deviation. Again: good results!&lt;/p&gt;

&lt;h1 id=&#34;variable-importance&#34;&gt;Variable Importance&lt;/h1&gt;

&lt;p&gt;One last thing to check is the &lt;em&gt;Variable Importance&lt;/em&gt;. CART is a &lt;em&gt;greedy&lt;/em&gt; algorithm, meaning that it chooses the best split at each node without any consideration of the potential benefit on the overall tree performance of choosing a different one. This way it is possible to end up with a tree with some &lt;em&gt;apparently relevant&lt;/em&gt; variables missing.&lt;/p&gt;

&lt;p&gt;The &lt;em&gt;Variable Importance&lt;/em&gt; in &lt;code&gt;rpart&lt;/code&gt; is calculated not only taking into account the goodness of the split for variables that are actually in the tree, but also for the &lt;code&gt;surrogate&lt;/code&gt; variables (the variables used in case the main variable is missing for an observation). We can see next that there are many more variables than in the original tree.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;winner_rpart$variable.importance %&amp;gt;%
  data_frame(variable = names(.), importance = .) %&amp;gt;%
  mutate(importance = importance / sum(importance)) %&amp;gt;%
  top_n(20) %&amp;gt;%
  ggplot(aes(x = importance,
             y = reorder(variable, importance))) +
  geom_point() +
  labs(title = &amp;quot;Importance of county characteristics \n in determining the most voted candidate&amp;quot;,
       subtitle = &amp;quot;(20 most relevant, Rpart, scaled to sum 1)&amp;quot;) +
  theme_bw() +
  theme(axis.title.y = element_blank(),
        plot.title = element_text(hjust = 0.5),
        plot.subtitle = element_text(hjust = 0.5),
        axis.line = element_line(colour = &amp;quot;grey&amp;quot;),
        panel.grid.major = element_blank(), panel.border = element_blank()) +
  geom_segment(aes(x = -Inf, y = reorder(variable, importance),
                   xend = importance, yend = reorder(variable, importance)),
               size = 0.2)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;img src=&#34;/post/what-demographics-voted-for-trump/2017-01-23-what-demographics-voted-for-trump/variable_importance-1.png&#34; title=&#34;plot of chunk variable_importance&#34; alt=&#34;plot of chunk variable_importance&#34; style=&#34;margin: auto;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;The most relevant characteristic is &lt;code&gt;white_alone&lt;/code&gt;, the percentage of &lt;em&gt;white&lt;/em&gt; population with &lt;em&gt;no Hispanic or Latin origin&lt;/em&gt;. In the top 7 almost every characteristic is about race or origin, so it is probably one of the key aspects to study if we want to understand this phenomenon (no surprises here).&lt;/p&gt;

&lt;h1 id=&#34;what-now&#34;&gt;What now?&lt;/h1&gt;

&lt;p&gt;This was my first approach to the subject, I am already doing some extra analysis on the &lt;em&gt;race&lt;/em&gt; phenomenon, so I&amp;rsquo;ll be probably posting more on this soon.&lt;/p&gt;

&lt;p&gt;If you made it until here: &lt;strong&gt;thank you!&lt;/strong&gt; I tried to keep it simple and intuitive, covering all relevant aspects when estimating this kind of models. The R Markdown I used to generate this post is &lt;a href=&#34;https://github.com/d4tagirl/d4tagirl.com/blob/master/_source/what-demographics-voted-for-trump/2017-01-23-what-demographics-voted-for-trump.Rmd&#34;&gt;available here&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;If you see room for improvement or simply want to share something, please let me know by leaving your comments here or &lt;a href=&#34;https://twitter.com/intent/tweet?user_id=114258616&#34;&gt;mentioning me on Twitter&lt;/a&gt; :)&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>
